<UserControl x:Class="SengokuSLG.Views.MonthlySummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="{StaticResource BrushPanelBg}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="30" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="600">
            <StackPanel>
                <TextBlock Text="【月次報告】" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20" Foreground="{StaticResource BrushAccent}"/>
                
                <TextBlock Text="今月の収支報告を行います。" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="・村A収入" FontSize="16"/>
                    <TextBlock Grid.Column="1" Text="{Binding Service.VillageA.Income, StringFormat='{}{0}貫'}" FontSize="16" FontWeight="Bold"/>
                </Grid>

                <Grid Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="・村B収入" FontSize="16"/>
                    <TextBlock Grid.Column="1" Text="{Binding Service.VillageB.Income, StringFormat='{}{0}貫'}" FontSize="16" FontWeight="Bold"/>
                </Grid>

                <Separator Margin="0,0,0,20" Background="{StaticResource BrushBorder}"/>

                <TextBlock Text="主君からの評価" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock Text="公務の実施を確認しました。" FontSize="14" Margin="0,0,0,5"/>
                <TextBlock Text="評価が上昇しました。" FontSize="14" Foreground="{StaticResource BrushAccent}" FontWeight="Bold"/>

                <Button Content="次月へ" Command="{Binding NextMonthCommand}" Style="{StaticResource ButtonStyleWood}" HorizontalAlignment="Center" Margin="0,30,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
