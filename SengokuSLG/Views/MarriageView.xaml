<UserControl x:Class="SengokuSLG.Views.MarriageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BrushBgBase}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="婚姻候補一覧" Style="{StaticResource TextHeader}" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Offers}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource BrushSurface}" CornerRadius="4" Margin="0,0,0,10" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding SourceHouseName}" FontSize="14" Foreground="{StaticResource BrushTextSecondary}"/>
                                    <TextBlock Text="{Binding CandidatePersonName}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,5"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                        <TextBlock Text="家格差: " Foreground="{StaticResource BrushTextSecondary}"/>
                                        <TextBlock Text="{Binding RankDifference}" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,15,0"/>
                                        <TextBlock Text="持参金: " Foreground="{StaticResource BrushTextSecondary}"/>
                                        <TextBlock Text="{Binding Dowry, StringFormat={}{0}貫}" Foreground="{StaticResource BrushTextPrimary}"/>
                                    </StackPanel>
                                </StackPanel>
                                <Button Grid.Column="1" Content="交渉" Command="{Binding DataContext.NegotiateCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Style="{StaticResource ButtonPrimary}" Width="100" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" HorizontalAlignment="Left" Width="120" Margin="0,20,0,0"/>
    </Grid>
</UserControl>
