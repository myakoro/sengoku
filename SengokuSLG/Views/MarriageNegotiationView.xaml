<UserControl x:Class="SengokuSLG.Views.MarriageNegotiationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BrushBgBase}">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="縁談交渉" Style="{StaticResource TextHeader}" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1">
            <Border Background="{StaticResource BrushSurface}" CornerRadius="4" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="相手方" FontSize="14" Foreground="{StaticResource BrushTextSecondary}"/>
                    <TextBlock Text="{Binding Offer.SourceHouseName}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}" Margin="0,5,0,15"/>
                    
                    <TextBlock Text="候補者" FontSize="14" Foreground="{StaticResource BrushTextSecondary}"/>
                    <TextBlock Text="{Binding Offer.CandidatePersonName}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushAccentGold}" Margin="0,5,0,15"/>
                    
                    <Grid Margin="0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="持参金" Foreground="{StaticResource BrushTextSecondary}"/>
                            <TextBlock Text="{Binding Offer.Dowry, StringFormat={}{0}貫}" FontSize="18" Foreground="{StaticResource BrushTextPrimary}"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="家格差" Foreground="{StaticResource BrushTextSecondary}"/>
                            <TextBlock Text="{Binding Offer.RankDifference}" FontSize="18" Foreground="{StaticResource BrushTextPrimary}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

            <TextBlock Text="成功確率" Foreground="{StaticResource BrushTextSecondary}" Margin="0,0,0,5"/>
            <Grid Height="24">
                <ProgressBar Value="{Binding SuccessProbability, Mode=OneWay}" Maximum="100" Background="{StaticResource BrushSurface}" Foreground="{StaticResource BrushAccentGold}" BorderThickness="0"/>
                <TextBlock Text="{Binding SuccessProbability, StringFormat={}{0:F0}%, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{StaticResource BrushTextPrimary}" FontWeight="Bold"/>
            </Grid>
            <TextBlock Text="※家格差、役職、親密度により変動します" FontSize="12" Foreground="{StaticResource BrushTextSecondary}" Margin="0,5,0,0"/>

        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="120" Margin="0,0,20,0"/>
            <Button Content="交渉開始" Command="{Binding ExecuteCommand}" Style="{StaticResource ButtonPrimary}" Width="160"/>
        </StackPanel>
    </Grid>
</UserControl>
