<UserControl x:Class="SengokuSLG.Views.SuccessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BrushBgBase}">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="世代交代" Style="{StaticResource TextHeader}" Margin="0,0,0,30" Foreground="Red"/>

        <StackPanel Grid.Row="1">
            <TextBlock Text="当主が死亡しました。以下の者が跡を継ぎます。" FontSize="16" Foreground="{StaticResource BrushTextPrimary}" Margin="0,0,0,20"/>

            <Border Background="{StaticResource BrushSurface}" CornerRadius="4" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="新当主" FontSize="14" Foreground="{StaticResource BrushTextSecondary}"/>
                    <TextBlock Text="{Binding Heir.Name}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushAccentGold}" Margin="0,5,0,15"/>
                    
                    <TextBlock Text="継承内容" FontSize="14" Foreground="{StaticResource BrushTextSecondary}" Margin="0,10,0,5"/>
                    <TextBlock Text="・家格: 100%継承" Foreground="{StaticResource BrushTextPrimary}"/>
                    <TextBlock Text="・借金: 60%継承" Foreground="{StaticResource BrushTextPrimary}"/>
                    <TextBlock Text="・信用: 90%継承" Foreground="{StaticResource BrushTextPrimary}"/>
                    <TextBlock Text="・人脈: 100%継承" Foreground="{StaticResource BrushTextPrimary}"/>
                </StackPanel>
            </Border>

            <Border Background="#33FF0000" CornerRadius="4" Padding="15">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠" Foreground="Red" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="家中忠誠が低下します" Foreground="Red" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <Button Grid.Row="2" Content="継承する" Command="{Binding ConfirmCommand}" Style="{StaticResource ButtonPrimary}" HorizontalAlignment="Center" Width="200" Margin="0,30,0,0"/>
    </Grid>
</UserControl>
