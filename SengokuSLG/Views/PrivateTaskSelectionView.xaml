<UserControl x:Class="SengokuSLG.Views.PrivateTaskSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="TaskCardStyle" TargetType="Border">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#f0e6d2" Offset="0"/>
                        <GradientStop Color="#e7d8b1" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#8b4513"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#1d3557"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="3" Opacity="0.15"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="#f8f3e7">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Border Grid.Row="0" Background="{StaticResource BrushHeader}" Padding="15" BorderBrush="#8b4513" BorderThickness="0,0,0,2">
            <TextBlock Text="私事を選ぶ" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#2b2b2b"/>
        </Border>

        <!-- Task List -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel MaxWidth="600">
                <!-- 知行村の開発 -->
                <Border Style="{StaticResource TaskCardStyle}">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding ExecuteVillageDevelopmentCommand}" CommandParameter="村A"/>
                    </Border.InputBindings>
                    <StackPanel>
                        <TextBlock Text="知行村の開発（村A）" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="村Aの発展に投資する" FontSize="14" Foreground="#555" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="金-10" FontSize="16" FontWeight="Bold" Foreground="#c73940" Margin="0,0,15,0"/>
                            <TextBlock Text="開発+2 / 人口+5" FontSize="13" FontWeight="Bold" Foreground="#2b2b2b"/>
                        </StackPanel>
                        <TextBlock Text="功績なし" FontSize="12" FontStyle="Italic" Foreground="#888" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- 道整備 -->
                <Border Style="{StaticResource TaskCardStyle}">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding ExecuteRoadMaintenanceCommand}" CommandParameter="村A"/>
                    </Border.InputBindings>
                    <StackPanel>
                        <TextBlock Text="道整備（村A）" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="村A周辺の道を整備する" FontSize="14" Foreground="#555" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="金-5" FontSize="16" FontWeight="Bold" Foreground="#c73940" Margin="0,0,15,0"/>
                            <TextBlock Text="開発+1" FontSize="13" FontWeight="Bold" Foreground="#2b2b2b"/>
                        </StackPanel>
                        <TextBlock Text="功績なし" FontSize="12" FontStyle="Italic" Foreground="#888" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- 村商人との取引 -->
                <Border Style="{StaticResource TaskCardStyle}">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding ExecuteVillageMerchantTradeCommand}" CommandParameter="村A"/>
                    </Border.InputBindings>
                    <StackPanel>
                        <TextBlock Text="村商人との取引（村A）" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="村Aの商人と取引を行う" FontSize="14" Foreground="#555" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="金+10" FontSize="16" FontWeight="Bold" Foreground="#2d7a3e" Margin="0,0,15,0"/>
                        </StackPanel>
                        <TextBlock Text="功績なし" FontSize="12" FontStyle="Italic" Foreground="#888" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <!-- 名産開発の準備 -->
                <Border Style="{StaticResource TaskCardStyle}">
                    <Border.InputBindings>
                        <MouseBinding Gesture="LeftClick" Command="{Binding ExecuteSpecialtyPreparationCommand}" CommandParameter="村A"/>
                    </Border.InputBindings>
                    <StackPanel>
                        <TextBlock Text="名産開発の準備（村A）" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
                        <TextBlock Text="村Aで名産品の開発準備を行う" FontSize="14" Foreground="#555" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="金-15" FontSize="16" FontWeight="Bold" Foreground="#c73940" Margin="0,0,15,0"/>
                            <TextBlock Text="開発+1" FontSize="13" FontWeight="Bold" Foreground="#2b2b2b"/>
                        </StackPanel>
                        <TextBlock Text="功績なし" FontSize="12" FontStyle="Italic" Foreground="#888" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>

        <!-- Back Button -->
        <Button Grid.Row="2" Command="{Binding BackCommand}" Content="戻る" 
                Style="{StaticResource StyleNavButton}" Width="200" Height="50" Margin="20"/>
    </Grid>
</UserControl>
