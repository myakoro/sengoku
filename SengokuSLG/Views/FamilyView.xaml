<UserControl x:Class="SengokuSLG.Views.FamilyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{StaticResource BrushBgBase}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="子供・元服管理" Style="{StaticResource TextHeader}" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Children}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource BrushSurface}" CornerRadius="4" Margin="0,0,0,10" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushTextPrimary}"/>
                                    <TextBlock Text="{Binding Age, StringFormat={}{0}歳}" FontSize="14" Foreground="{StaticResource BrushTextSecondary}"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                                    <Border Background="{StaticResource BrushAccentGold}" CornerRadius="2" Padding="5,2" Visibility="{Binding IsAdult, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="成人" Foreground="Black" FontSize="12"/>
                                    </Border>
                                    <Border Background="{StaticResource BrushTextSecondary}" CornerRadius="2" Padding="5,2" Margin="5,0,0,0" Visibility="{Binding IsGenpuku, Converter={StaticResource BooleanToVisibilityConverter}}">
                                        <TextBlock Text="元服済" Foreground="White" FontSize="12"/>
                                    </Border>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" HorizontalAlignment="Left" Width="120" Margin="0,20,0,0"/>
    </Grid>
</UserControl>
