<UserControl x:Class="SengokuSLG.Views.VassalDetailView_New"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Background="{StaticResource BrushBgBase}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,2" Padding="20,15">
            <Grid>
                <TextBlock Text="🐴 家臣詳細" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="編集" Style="{StaticResource ButtonPrimary}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- 左カラム -->
                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <!-- 基本情報 -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="基本情報" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            <TextBlock Text="🐴 加藤八郎（馬上衆）" FontSize="18" Margin="0,0,0,8"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" TextElement.FontSize="11">
                                    <TextBlock Text="年齢: 28歳" Margin="0,0,0,4"/>
                                    <TextBlock Text="役職: 馬上衆" Margin="0,0,0,4"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1" TextElement.FontSize="11">
                                    <TextBlock Text="出身: 村A (001)" Margin="0,0,0,4"/>
                                    <TextBlock Text="所属: 小隊1" Margin="0,0,0,4"/>
                                </StackPanel>
                            </Grid>
                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                <Border Background="#d4edda" BorderBrush="#c3e6cb" BorderThickness="1" CornerRadius="3" Padding="4,2" Margin="0,0,4,0">
                                    <TextBlock Text="騎乗✓" FontSize="10" Foreground="#155724"/>
                                </Border>
                                <Border Background="#1976d2" CornerRadius="3" Padding="4,2">
                                    <TextBlock Text="馬保有✓" FontSize="10" Foreground="White"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 4能力 -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="4能力" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            
                            <!-- 戦闘力 -->
                            <StackPanel Margin="0,0,0,10">
                                <Grid Margin="0,0,0,2">
                                    <TextBlock Text="戦闘力" FontSize="11"/>
                                    <TextBlock Text="18/25" FontSize="11" HorizontalAlignment="Right"/>
                                </Grid>
                                <Border Background="#e0e0e0" Height="16" CornerRadius="3">
                                    <Border Background="#66BB6A" Width="180" HorizontalAlignment="Left" CornerRadius="3">
                                        <TextBlock Text="18" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Border>
                            </StackPanel>

                            <!-- 統率 -->
                            <StackPanel Margin="0,0,0,10">
                                <Grid Margin="0,0,0,2">
                                    <TextBlock Text="統率" FontSize="11"/>
                                    <TextBlock Text="12/25" FontSize="11" HorizontalAlignment="Right"/>
                                </Grid>
                                <Border Background="#e0e0e0" Height="16" CornerRadius="3">
                                    <Border Background="#1976D2" Width="120" HorizontalAlignment="Left" CornerRadius="3">
                                        <TextBlock Text="12" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Border>
                            </StackPanel>

                            <!-- 政務 -->
                            <StackPanel Margin="0,0,0,10">
                                <Grid Margin="0,0,0,2">
                                    <TextBlock Text="政務" FontSize="11"/>
                                    <TextBlock Text="5/25" FontSize="11" HorizontalAlignment="Right"/>
                                </Grid>
                                <Border Background="#e0e0e0" Height="16" CornerRadius="3">
                                    <Border Background="#FF9800" Width="50" HorizontalAlignment="Left" CornerRadius="3">
                                        <TextBlock Text="5" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Border>
                            </StackPanel>

                            <!-- 知略 -->
                            <StackPanel>
                                <Grid Margin="0,0,0,2">
                                    <TextBlock Text="知略" FontSize="11"/>
                                    <TextBlock Text="8/25" FontSize="11" HorizontalAlignment="Right"/>
                                </Grid>
                                <Border Background="#e0e0e0" Height="16" CornerRadius="3">
                                    <Border Background="#9C27B0" Width="80" HorizontalAlignment="Left" CornerRadius="3">
                                        <TextBlock Text="8" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- 実戦度・訓練度 -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                        <StackPanel>
                            <TextBlock Text="実戦度・訓練度" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            
                            <!-- 実戦度 -->
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock FontSize="11" Margin="0,0,0,2">
                                    <Run Text="実戦度 65/100 "/>
                                    <Run Text="補正: 0.925" Foreground="#666"/>
                                </TextBlock>
                                <Border Background="#e0e0e0" Height="16" CornerRadius="3">
                                    <Border Background="#1565C0" Width="162" HorizontalAlignment="Left" CornerRadius="3">
                                        <TextBlock Text="65" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Border>
                            </StackPanel>

                            <!-- 訓練度 -->
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock FontSize="11" Margin="0,0,0,2">
                                    <Run Text="訓練度 78/100 "/>
                                    <Run Text="補正: 0.890" Foreground="#666"/>
                                </TextBlock>
                                <Border Background="#e0e0e0" Height="16" CornerRadius="3">
                                    <Border Background="#2d7a3e" Width="195" HorizontalAlignment="Left" CornerRadius="3">
                                        <TextBlock Text="78" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Border>
                            </StackPanel>

                            <TextBlock Text="隊平均訓練度: 75/100" FontSize="11" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- 右カラム -->
                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <!-- 一家構造 -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                        <StackPanel>
                            <TextBlock Text="一家構造（家内従僕）" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            <TextBlock Text="主人: 加藤八郎" FontSize="11" Margin="0,0,0,10"/>
                            <DataGrid AutoGenerateColumns="False" 
                                      Background="White" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                                      RowBackground="White" AlternatingRowBackground="#f9f9f9"
                                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="名前" Width="*"/>
                                    <DataGridTextColumn Header="年齢" Width="60"/>
                                    <DataGridTextColumn Header="戦闘力" Width="70"/>
                                    <DataGridTemplateColumn Header="状態" Width="80">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="#6c757d" CornerRadius="3" Padding="4,2">
                                                    <TextBlock Text="待機" FontSize="10" Foreground="White" HorizontalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                            <TextBlock Text="従僕数: 2人" FontSize="11" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- 戦歴 -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                        <StackPanel>
                            <TextBlock Text="戦歴" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                                    <TextBlock Text="総戦功" FontSize="11" Foreground="#666" HorizontalAlignment="Center"/>
                                    <TextBlock Text="450点" FontSize="18" FontWeight="Bold" Foreground="#c73940" HorizontalAlignment="Center"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock Text="参戦回数" FontSize="11" Foreground="#666" HorizontalAlignment="Center"/>
                                    <TextBlock Text="12回" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                                    <TextBlock Text="勝敗" FontSize="11" Foreground="#666" HorizontalAlignment="Center"/>
                                    <TextBlock Text="勝8 / 敗4" FontSize="11" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>
                            <DataGrid AutoGenerateColumns="False" 
                                      Background="White" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                                      RowBackground="White" AlternatingRowBackground="#f9f9f9"
                                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="日付" Width="90"/>
                                    <DataGridTextColumn Header="戦場" Width="*"/>
                                    <DataGridTextColumn Header="戦功" Width="80"/>
                                    <DataGridTemplateColumn Header="結果" Width="80">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border Background="#28a745" CornerRadius="3" Padding="4,2">
                                                    <TextBlock Text="大勝" FontSize="10" Foreground="White" HorizontalAlignment="Center"/>
                                                </Border>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
