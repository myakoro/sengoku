<UserControl x:Class="SengokuSLG.Views.CompanyManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Background="{StaticResource BrushBgBase}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,2" Padding="20,15">
            <Grid>
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" VerticalAlignment="Center">
                    <Run Text="⚔ "/>
                    <Run Text="{Binding SelectedCompany.Name}"/>
                    <Run Text=" - 中隊管理"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="詳細" Style="{StaticResource ButtonPrimary}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Company Summary -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                    <TextBlock FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}">
                        <Run Text="組頭: "/>
                        <Run Text="{Binding SelectedCompany.CommanderName}"/>
                        <Run Text=" / 総兵力: " Foreground="{StaticResource BrushTextSecondary}"/>
                        <Run Text="{Binding SelectedCompany.CombatantCount, Mode=OneWay}" Foreground="{StaticResource BrushTextSecondary}"/>
                        <Run Text="人 / 性格: " Foreground="{StaticResource BrushTextSecondary}"/>
                        <Run Text="{Binding SelectedCompany.Personality}" Foreground="{StaticResource BrushTextSecondary}"/>
                    </TextBlock>
                </Border>

                <!-- Squad List -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                    <StackPanel>
                        <TextBlock Text="小隊一覧（4衆）" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                        
                        <ItemsControl ItemsSource="{Binding SelectedCompany.Squads, Mode=OneWay}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#f9f9f9" BorderBrush="#1976d2" BorderThickness="0,0,0,3" CornerRadius="4" Padding="10" Margin="0,0,10,10">
                                        <StackPanel>
                                            <Grid>
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,5"/>
                                                <Button Content="詳細" Command="{Binding DataContext.NavigateToSquadCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                        Style="{StaticResource ButtonSecondary}" Padding="5,2" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Top"/>
                                            </Grid>
                                            <TextBlock FontSize="12" Foreground="{StaticResource BrushTextSecondary}">
                                                <Run Text="小頭: "/>
                                                <Run Text="{Binding CommanderName}"/>
                                                <Run Text=" / 兵力: "/>
                                                <Run Text="{Binding CombatantCount, Mode=OneWay}"/>
                                            </TextBlock>
                                            
                                            <!-- Morale Bar -->
                                            <Grid Margin="0,5,0,2">
                                                <ProgressBar Value="{Binding Morale}" Maximum="100" Height="16" Foreground="{StaticResource BrushAccentGreen}" Background="#e0e0e0" BorderThickness="0"/>
                                                <TextBlock Text="{Binding Morale, StringFormat='士気: {0}'}" FontSize="10" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                            
                                            <!-- Fatigue Bar -->
                                            <Grid>
                                                <ProgressBar Value="{Binding Fatigue}" Maximum="100" Height="16" Foreground="{StaticResource BrushAccentRed}" Background="#e0e0e0" BorderThickness="0"/>
                                                <TextBlock Text="{Binding Fatigue, StringFormat='疲労: {0}'}" FontSize="10" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
