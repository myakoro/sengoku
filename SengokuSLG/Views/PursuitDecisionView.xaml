<UserControl x:Class="SengokuSLG.Views.PursuitDecisionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Background="{StaticResource BrushBgBase}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,2" Padding="20,15">
            <Grid>
                <TextBlock Text="🏹 追撃判定" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="実行" Style="{StaticResource ButtonPrimary}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 追撃条件チェックと追撃強度 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- 追撃条件チェック -->
                    <Border Grid.Column="0" Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="追撃条件チェック" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            <Border Background="#F5F5F5" CornerRadius="4" Padding="10">
                                <StackPanel TextElement.FontSize="11">
                                    <TextBlock Margin="0,0,0,4">☑ 自軍疲労 &lt; 60 (現在: 35)</TextBlock>
                                    <TextBlock Margin="0,0,0,4">☑ 自軍士気 ≥ 50 (現在: 75)</TextBlock>
                                    <TextBlock Margin="0,0,0,4">☑ 敵が完全崩壊（士気0）</TextBlock>
                                    <TextBlock>☑ 地形が追撃可能</TextBlock>
                                </StackPanel>
                            </Border>
                            <Border Background="#d4edda" BorderBrush="#c3e6cb" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,10,0,0">
                                <TextBlock FontSize="12" Foreground="#155724">
                                    <Run Text="判定: 追撃可能 ✓" FontWeight="Bold"/>
                                </TextBlock>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- 追撃強度 -->
                    <Border Grid.Column="1" Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="追撃強度" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            <Border Background="White" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" Margin="10">
                                        <TextBlock Text="自軍疲労: 35" FontSize="11" Margin="0,0,0,2"/>
                                        <TextBlock Text="自軍士気: 75" FontSize="11" Margin="0,0,0,2"/>
                                        <TextBlock Text="敵壊乱度: 高" FontSize="11" Margin="0,0,0,2"/>
                                        <TextBlock Text="地形: 平地" FontSize="11"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Margin="10">
                                        <TextBlock Text="→ 低疲労 (+1)" FontSize="11" Margin="0,0,0,2"/>
                                        <TextBlock Text="→ 高士気 (+1)" FontSize="11" Margin="0,0,0,2"/>
                                        <TextBlock Text="→ 高壊乱 (+1)" FontSize="11" Margin="0,0,0,2"/>
                                        <TextBlock Text="→ 追撃容易 (+0)" FontSize="11"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <Border Margin="0,10,0,0">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#FF9800" Offset="0"/>
                                        <GradientStop Color="#F57C00" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border CornerRadius="6" Padding="15">
                                    <TextBlock Text="追撃強度: P2（中追撃） 🗡" FontSize="22" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                </Border>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- 損耗予測 -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="損耗予測" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock FontSize="11" Margin="0,0,0,4">
                                    <Run Text="敵軍残存兵力: " FontWeight="Bold"/>
                                    <Run Text="250人"/>
                                </TextBlock>
                                <TextBlock FontSize="11" Margin="0,0,0,4">
                                    <Run Text="予測損耗率: " FontWeight="Bold"/>
                                    <Run Text="10〜20% (P2)"/>
                                </TextBlock>
                                <TextBlock FontSize="11">
                                    <Run Text="予測損耗人数: " FontWeight="Bold"/>
                                    <Run Text="25〜50人"/>
                                </TextBlock>
                            </StackPanel>

                            <Border Grid.Column="1" Background="#F5F5F5" CornerRadius="4" Padding="10">
                                <StackPanel TextElement.FontSize="11">
                                    <TextBlock Text="内訳予測:" FontWeight="Bold" Margin="0,0,0,4"/>
                                    <TextBlock Margin="0,0,0,2">・死亡: 8〜12人</TextBlock>
                                    <TextBlock Margin="0,0,0,2">・廃兵: 5〜8人</TextBlock>
                                    <TextBlock Margin="0,0,0,2">・負傷: 10〜20人</TextBlock>
                                    <TextBlock>・逃散: 2〜10人</TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 決定ボタン -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="追撃を実行する" Style="{StaticResource ButtonPrimary}" Padding="20,15" FontSize="16"/>
                        <Button Grid.Column="2" Content="追撃を見送る" Style="{StaticResource ButtonSecondary}" Padding="20,15" FontSize="16"/>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
