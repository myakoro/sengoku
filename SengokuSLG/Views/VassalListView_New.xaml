<UserControl x:Class="SengokuSLG.Views.VassalListView_New"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Background="{StaticResource BrushBgBase}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,2" Padding="20,15">
            <Grid>
                <TextBlock Text="🏯 役職一覧" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="編成管理" Style="{StaticResource ButtonPrimary}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 組織図（ピラミッド構造） -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="🏯 組織図（ピラミッド構造）" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,20"/>
                        
                        <!-- 足軽大将 -->
                        <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                            <Border>
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#c73940" Offset="0"/>
                                        <GradientStop Color="#A02F17" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Border CornerRadius="8" Padding="15,30">
                                    <StackPanel>
                                        <TextBlock Text="🏯 足軽大将（指揮上限: 300）" FontSize="16" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                        <TextBlock Text="山田太郎" FontSize="18" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
                                        <TextBlock Text="統率85 / 政務60 / 知略70" FontSize="11" Foreground="White" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </Border>
                            <Rectangle Width="2" Height="20" Fill="#999"/>
                        </StackPanel>

                        <!-- 組頭 -->
                        <StackPanel HorizontalAlignment="Center" Margin="0,0,0,20">
                            <TextBlock Text="⚔ 組頭（指揮上限: 100）" FontSize="12" Foreground="#666" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <Grid MaxWidth="900">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- 佐藤組 -->
                                <Border Grid.Column="0" Background="#E3F2FD" BorderBrush="#1976d2" BorderThickness="0,0,0,4" CornerRadius="6" Padding="12" Margin="0,0,6,0">
                                    <StackPanel>
                                        <TextBlock Text="佐藤組" FontSize="14" FontWeight="Bold" Foreground="#1976d2" Margin="0,0,0,2"/>
                                        <TextBlock FontWeight="Bold" Margin="0,0,0,4">
                                            <Run Text="佐藤次郎 "/>
                                            <Border Background="#6c757d" CornerRadius="3" Padding="4,2" VerticalAlignment="Center">
                                                <TextBlock Text="標準" FontSize="10" Foreground="White"/>
                                            </Border>
                                        </TextBlock>
                                        <TextBlock Text="統率70 / 政務50 / 知略55" FontSize="11"/>
                                        <TextBlock Text="配下: 小隊4つ" FontSize="11" Foreground="#666"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 鈴木組 -->
                                <Border Grid.Column="1" Background="#E8F5E9" BorderBrush="#2d7a3e" BorderThickness="0,0,0,4" CornerRadius="6" Padding="12" Margin="0,0,6,0">
                                    <StackPanel>
                                        <TextBlock Text="鈴木組" FontSize="14" FontWeight="Bold" Foreground="#2d7a3e" Margin="0,0,0,2"/>
                                        <TextBlock FontWeight="Bold" Margin="0,0,0,4">
                                            <Run Text="鈴木三郎 "/>
                                            <Border Background="#28a745" CornerRadius="3" Padding="4,2" VerticalAlignment="Center">
                                                <TextBlock Text="慎重" FontSize="10" Foreground="White"/>
                                            </Border>
                                        </TextBlock>
                                        <TextBlock Text="統率68 / 政務55 / 知略60" FontSize="11"/>
                                        <TextBlock Text="配下: 小隊4つ" FontSize="11" Foreground="#666"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- 田中組 -->
                                <Border Grid.Column="2" Background="#FFEBEE" BorderBrush="#c73940" BorderThickness="0,0,0,4" CornerRadius="6" Padding="12">
                                    <StackPanel>
                                        <TextBlock Text="田中組" FontSize="14" FontWeight="Bold" Foreground="#c73940" Margin="0,0,0,2"/>
                                        <TextBlock FontWeight="Bold" Margin="0,0,0,4">
                                            <Run Text="田中四郎 "/>
                                            <Border Background="#dc3545" CornerRadius="3" Padding="4,2" VerticalAlignment="Center">
                                                <TextBlock Text="猪突" FontSize="10" Foreground="White"/>
                                            </Border>
                                        </TextBlock>
                                        <TextBlock Text="統率75 / 政務45 / 知略50" FontSize="11"/>
                                        <TextBlock Text="配下: 小隊4つ" FontSize="11" Foreground="#666"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                            <Rectangle Width="2" Height="20" Fill="#999" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- 小頭 -->
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="🗡 小頭（指揮上限: 25）- 最大12衆" FontSize="12" Foreground="#666" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="※実装簡略化のため、小頭12衆の詳細表示は省略" FontSize="11" Foreground="#999" HorizontalAlignment="Center" FontStyle="Italic"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- 馬上衆 -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="🐴 馬上衆（騎乗可能）" FontSize="14" FontWeight="Bold" Foreground="#8b4513" Margin="0,0,0,10"/>
                        <TextBlock Text="※馬上衆の詳細リストは実装簡略化のため省略" FontSize="11" Foreground="#999" FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <!-- 徒士 -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                    <StackPanel>
                        <TextBlock Text="⚔ 徒士（歩兵）" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                        <TextBlock Text="※徒士の詳細リストは実装簡略化のため省略" FontSize="11" Foreground="#999" FontStyle="Italic"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
