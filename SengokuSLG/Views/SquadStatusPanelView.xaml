<UserControl x:Class="SengokuSLG.Views.SquadStatusPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Background="{StaticResource BrushBgBase}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,2" Padding="20,15">
            <Grid>
                <TextBlock Text="📊 隊状態パネル" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="更新" Style="{StaticResource ButtonPrimary}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- 階層選択タブ -->
                <Border Background="{StaticResource BrushBgPanel}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="6,6,0,0" Padding="0" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <Button Content="大隊" Style="{StaticResource StyleNavButton}" Background="{StaticResource BrushBgCard}" Padding="15,10" FontSize="14" FontWeight="Bold"/>
                        <Button Content="中隊" Style="{StaticResource StyleNavButton}" Padding="15,10" FontSize="14"/>
                        <Button Content="小隊" Style="{StaticResource StyleNavButton}" Padding="15,10" FontSize="14"/>
                    </StackPanel>
                </Border>

                <!-- 大隊一覧 - 2カラム -->
                <ItemsControl ItemsSource="{Binding Battalions}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,12,12">
                                <StackPanel>
                                    <TextBlock FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10">
                                        <Run Text="{Binding Name}"/>
                                        <Run Text="（足軽大将："/>
                                        <Run Text="{Binding CommanderName}"/>
                                        <Run Text="）"/>
                                    </TextBlock>

                                    <!-- 訓練度と実戦度 -->
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 訓練度 -->
                                        <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                            <TextBlock FontSize="11" Foreground="#666">
                                                <Run Text="訓練度 "/>
                                                <Run Text="{Binding Training}"/>
                                                <Run Text="/100"/>
                                            </TextBlock>
                                            <Border Background="#e0e0e0" Height="16" CornerRadius="3" Margin="0,2,0,2">
                                                <Border Background="#2d7a3e" Width="{Binding TrainingBarWidth}" HorizontalAlignment="Left" CornerRadius="3">
                                                    <TextBlock Text="{Binding Training}" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Border>
                                            <TextBlock FontSize="11" Foreground="#666">
                                                <Run Text="補正: "/>
                                                <Run Text="{Binding TrainingModifier, StringFormat=F3}"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <!-- 実戦度 -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock FontSize="11" Foreground="#666">
                                                <Run Text="実戦度 "/>
                                                <Run Text="{Binding Experience}"/>
                                                <Run Text="/100"/>
                                            </TextBlock>
                                            <Border Background="#e0e0e0" Height="16" CornerRadius="3" Margin="0,2,0,2">
                                                <Border Background="#1565C0" Width="{Binding ExperienceBarWidth}" HorizontalAlignment="Left" CornerRadius="3">
                                                    <TextBlock Text="{Binding Experience}" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Border>
                                            <TextBlock FontSize="11" Foreground="#666">
                                                <Run Text="補正: "/>
                                                <Run Text="{Binding ExperienceModifier, StringFormat=F3}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>

                                    <!-- 疲労と士気 -->
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- 疲労 -->
                                        <StackPanel Grid.Column="0" Margin="0,0,6,0">
                                            <TextBlock FontSize="11" Foreground="#666">
                                                <Run Text="疲労 "/>
                                                <Run Text="{Binding Fatigue}"/>
                                                <Run Text="/100 "/>
                                                <Run Text="✓" Foreground="#155724" FontWeight="Bold"/>
                                            </TextBlock>
                                            <Border Background="#e0e0e0" Height="16" CornerRadius="3" Margin="0,2,0,0">
                                                <Border Background="#c73940" Width="{Binding FatigueBarWidth}" HorizontalAlignment="Left" CornerRadius="3">
                                                    <TextBlock Text="{Binding Fatigue}" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Border>
                                        </StackPanel>

                                        <!-- 士気 -->
                                        <StackPanel Grid.Column="1">
                                            <TextBlock FontSize="11" Foreground="#666">
                                                <Run Text="士気 "/>
                                                <Run Text="{Binding Morale}"/>
                                                <Run Text="/100 "/>
                                                <Run Text="✓" Foreground="#155724" FontWeight="Bold"/>
                                            </TextBlock>
                                            <Border Background="#e0e0e0" Height="16" CornerRadius="3" Margin="0,2,0,0">
                                                <Border Background="#1976d2" Width="{Binding MoraleBarWidth}" HorizontalAlignment="Left" CornerRadius="3">
                                                    <TextBlock Text="{Binding Morale}" FontSize="10" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </Border>
                                        </StackPanel>
                                    </Grid>

                                    <!-- ボタン -->
                                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                        <Button Content="詳細" Style="{StaticResource ButtonSecondary}" Padding="8,4" FontSize="12" Margin="0,0,4,0"/>
                                        <Button Content="訓練" Style="{StaticResource ButtonSecondary}" Padding="8,4" FontSize="12" Margin="0,0,4,0"/>
                                        <Button Content="休息" Style="{StaticResource ButtonSecondary}" Padding="8,4" FontSize="12"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- 状態推移グラフ -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                    <StackPanel>
                        <TextBlock Text="状態推移グラフ（簡易版）" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                        <Border Background="#F5F5F5" BorderBrush="#999" BorderThickness="1" CornerRadius="6" Padding="30">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="📈 訓練度・実戦度・疲労・士気の推移グラフ" FontSize="12" Foreground="#666" HorizontalAlignment="Center"/>
                                <TextBlock Text="横軸: 1日前 → 12h前 → 6h前 → 3h前 → 現在 / 縦軸: 0〜100" FontSize="11" Foreground="#666" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                            </StackPanel>
                        </Border>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                            <TextBlock Text="━ 訓練度" FontSize="11" Foreground="#2E7D32" Margin="0,0,10,0"/>
                            <TextBlock Text="━ 実戦度" FontSize="11" Foreground="#1565C0" Margin="0,0,10,0"/>
                            <TextBlock Text="━ 疲労" FontSize="11" Foreground="#FF9800" Margin="0,0,10,0"/>
                            <TextBlock Text="━ 士気" FontSize="11" Foreground="#1976D2"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
