<UserControl x:Class="SengokuSLG.Views.VassalListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SengokuSLG.Views"
             xmlns:conv="clr-namespace:SengokuSLG.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Center Area -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with Back Button -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="家臣一覧" FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource BrushTextMain}"
                           TextDecorations="Underline" VerticalAlignment="Center"/>
                <Button Command="{Binding BackCommand}" Content="← メイン画面に戻る" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Style="{StaticResource StyleNavButton}" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"/>
            </Grid>

            <!-- Filter Bar -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="絞り込み:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                <Button Command="{Binding FilterCommand}" CommandParameter="All" Content="すべて" Style="{StaticResource StyleNavButton}" Margin="0,0,5,0" Background="#ddd"/>
                <Button Command="{Binding FilterCommand}" CommandParameter="Juboku" Content="従僕" Style="{StaticResource StyleNavButton}" Margin="0,0,5,0" Background="#ddd"/>
                <Button Command="{Binding FilterCommand}" CommandParameter="Toshi" Content="徒士" Style="{StaticResource StyleNavButton}" Margin="0,0,5,0" Background="#ddd"/>
                <Button Command="{Binding FilterCommand}" CommandParameter="Kumigashira" Content="組頭" Style="{StaticResource StyleNavButton}" Margin="0,0,5,0" Background="#ddd"/>
                <Button Command="{Binding FilterCommand}" CommandParameter="Busho" Content="武将" Style="{StaticResource StyleNavButton}" Margin="0,0,5,0" Background="#ddd"/>
            </StackPanel>

            <!-- Vassal List Table -->
            <Border Grid.Row="2" Background="{StaticResource BrushBgPanel}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Table Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/> <!-- Name -->
                            <ColumnDefinition Width="50"/>  <!-- Age -->
                            <ColumnDefinition Width="80"/>  <!-- Rank -->
                            <ColumnDefinition Width="60"/>  <!-- Combat -->
                            <ColumnDefinition Width="60"/>  <!-- Leadership -->
                            <ColumnDefinition Width="60"/>  <!-- Politics -->
                            <ColumnDefinition Width="60"/>  <!-- Intrigue -->
                            <ColumnDefinition Width="60"/>  <!-- Loyalty -->
                            <ColumnDefinition Width="80"/>  <!-- Achievement -->
                            <ColumnDefinition Width="*"/>   <!-- Action -->
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" Text="氏名" FontWeight="Bold"/>
                        <TextBlock Grid.Column="1" Text="年齢" FontWeight="Bold"/>
                        <TextBlock Grid.Column="2" Text="身分" FontWeight="Bold"/>
                        <TextBlock Grid.Column="3" Text="武芸" FontWeight="Bold" Foreground="{StaticResource BrushAccentRed}"/>
                        <TextBlock Grid.Column="4" Text="統率" FontWeight="Bold" Foreground="{StaticResource BrushAccentBlue}"/>
                        <TextBlock Grid.Column="5" Text="政務" FontWeight="Bold" Foreground="{StaticResource BrushAccentGreen}"/>
                        <TextBlock Grid.Column="6" Text="知略" FontWeight="Bold" Foreground="{StaticResource BrushBorder}"/>
                        <TextBlock Grid.Column="7" Text="忠誠" FontWeight="Bold"/>
                        <TextBlock Grid.Column="8" Text="功績" FontWeight="Bold"/>
                        <TextBlock Grid.Column="9" Text="操作" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </Grid>
                    
                    <ScrollViewer Grid.Row="1">
                        <ItemsControl ItemsSource="{Binding VassalView}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="5" Margin="0,0,0,5" Padding="10,5">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/>
                                                <ColumnDefinition Width="50"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="60"/>
                                                <ColumnDefinition Width="80"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Bold"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Age}" VerticalAlignment="Center"/>
                                            
                                            <Border Grid.Column="2" Background="{Binding Rank, Converter={StaticResource RankToColorConverter}}" CornerRadius="8" Padding="8,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Rank, Converter={StaticResource RankToTextConverter}}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                            </Border>

                                            <!-- Stats with Disclosure Check -->
                                            <TextBlock Grid.Column="3" VerticalAlignment="Center" FontWeight="Bold">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource AbilityDisclosureConverter}">
                                                        <Binding Path="AbilityCombat"/>
                                                        <Binding Path="AbilityDisclosure.CombatDisclosed"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Grid.Column="4" VerticalAlignment="Center" FontWeight="Bold">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource AbilityDisclosureConverter}">
                                                        <Binding Path="AbilityLeadership"/>
                                                        <Binding Path="AbilityDisclosure.LeadershipDisclosed"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Grid.Column="5" VerticalAlignment="Center" FontWeight="Bold">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource AbilityDisclosureConverter}">
                                                        <Binding Path="AbilityPolitics"/>
                                                        <Binding Path="AbilityDisclosure.PoliticsDisclosed"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>
                                            <TextBlock Grid.Column="6" VerticalAlignment="Center" FontWeight="Bold">
                                                <TextBlock.Text>
                                                    <MultiBinding Converter="{StaticResource AbilityDisclosureConverter}">
                                                        <Binding Path="AbilityIntrigue"/>
                                                        <Binding Path="AbilityDisclosure.IntrigueDisclosed"/>
                                                    </MultiBinding>
                                                </TextBlock.Text>
                                            </TextBlock>

                                            <TextBlock Grid.Column="7" Text="{Binding Loyalty}" VerticalAlignment="Center"/>
                                            <TextBlock Grid.Column="8" Text="{Binding Achievement}" VerticalAlignment="Center"/>

                                            <Button Grid.Column="9" Content="詳細" Command="{Binding DataContext.DetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding Id}" Padding="10,2" HorizontalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Right Sidebar -->
        <Border Grid.Column="1" Background="{StaticResource BrushBgPanel}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2,0,0,0" Padding="20">
            <ScrollViewer>
                <StackPanel>
                    <!-- Legend -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="■ 身分凡例" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushAccentRed}" Margin="0,0,0,12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Border Background="#888" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="従僕 (非戦闘員)"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Border Background="{StaticResource BrushAccentBlue}" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="徒士 (下級武士)"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                <Border Background="{StaticResource BrushAccentGreen}" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="組頭 (小隊長)"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <Border Background="{StaticResource BrushAccentRed}" Width="15" Height="15" Margin="0,0,10,0"/>
                                <TextBlock Text="武将 (指揮官)"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Ability Explanation -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="■ 能力値について" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushAccentRed}" Margin="0,0,0,12"/>
                            <Grid Margin="0,0,0,5">
                                <TextBlock Text="⚔ 武芸" Foreground="{StaticResource BrushAccentRed}" FontWeight="Bold"/>
                                <TextBlock Text="個人の戦闘力" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,5">
                                <TextBlock Text="🛡 統率" Foreground="{StaticResource BrushAccentBlue}" FontWeight="Bold"/>
                                <TextBlock Text="部隊の指揮能力" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,5">
                                <TextBlock Text="📜 政務" Foreground="{StaticResource BrushAccentGreen}" FontWeight="Bold"/>
                                <TextBlock Text="内政・開発の効果" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid>
                                <TextBlock Text="🎯 知略" Foreground="{StaticResource BrushBorder}" FontWeight="Bold"/>
                                <TextBlock Text="計略・交渉の成功率" HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Disclosure Status -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="15">
                        <StackPanel>
                            <TextBlock Text="■ 能力開示" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushAccentRed}" Margin="0,0,0,12"/>
                            <TextBlock Text="「?」は能力が未開示の状態です。" TextWrapping="Wrap" Margin="0,0,0,10"/>
                            <TextBlock Text="開示条件:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBlock Text="・任務の成功時" Margin="10,0,0,5"/>
                            <TextBlock Text="・補佐官への任命" Margin="10,0,0,5"/>
                            <TextBlock Text="・長い期間の奉公" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
