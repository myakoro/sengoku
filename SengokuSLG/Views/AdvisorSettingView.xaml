<UserControl x:Class="SengokuSLG.Views.AdvisorSettingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SengokuSLG.Views"
             xmlns:conv="clr-namespace:SengokuSLG.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Center Area -->
        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header with Back Button -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="補佐官設定" FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource BrushTextMain}"
                           TextDecorations="Underline" VerticalAlignment="Center"/>
                <Button Command="{Binding BackCommand}" Content="← メイン画面に戻る" 
                        HorizontalAlignment="Right" VerticalAlignment="Center"
                        Style="{StaticResource StyleNavButton}" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"/>
            </Grid>

            <!-- Current Advisor Section -->
            <Border Grid.Row="1" Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="■ 現在の補佐官" FontSize="18" FontWeight="Bold" Foreground="{StaticResource BrushAccentRed}" Margin="0,0,0,20"/>
                    
                    <!-- Show when advisor is appointed -->
                    <Grid Visibility="{Binding HasAdvisor, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                <TextBlock Text="{Binding CurrentAdvisor.Name}" FontSize="22" FontWeight="Bold" Margin="0,0,15,0"/>
                                <Border Background="{StaticResource BrushAccentBlue}" CornerRadius="10" Padding="10,2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding CurrentAdvisor.Rank, Converter={StaticResource RankToTextConverter}}" Foreground="White" FontSize="12" FontWeight="Bold"/>
                                </Border>
                            </StackPanel>
                        </StackPanel>

                        <!-- Dismiss Button -->
                        <Button Grid.Column="1" Command="{Binding DismissCommand}" Content="解任する" Padding="15,8" Background="#888" Foreground="White" BorderThickness="0" FontWeight="Bold"/>
                    </Grid>
                    
                    <!-- Show when no advisor -->
                    <TextBlock Text="補佐官は任命されていません" FontSize="16" Foreground="#888" 
                               Visibility="{Binding HasAdvisor, Converter={StaticResource InverseBooleanConverter}}"/>
                </StackPanel>
            </Border>

            <!-- Candidates List -->
            <Border Grid.Row="2" Background="{StaticResource BrushBgPanel}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="12" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Text="■ 補佐官候補（全家臣）" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                    
                    <ScrollViewer Grid.Row="1">
                        <ItemsControl ItemsSource="{Binding Vassals}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10" Padding="10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="120"/> <!-- Name -->
                                                <ColumnDefinition Width="80"/>  <!-- Rank -->
                                                <ColumnDefinition Width="60"/>  <!-- Combat -->
                                                <ColumnDefinition Width="60"/>  <!-- Leadership -->
                                                <ColumnDefinition Width="60"/>  <!-- Politics -->
                                                <ColumnDefinition Width="60"/>  <!-- Intrigue -->
                                                <ColumnDefinition Width="*"/>   <!-- Spacer -->
                                                <ColumnDefinition Width="Auto"/> <!-- Button -->
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Grid.Column="0" Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center"/>
                                            
                                            <Border Grid.Column="1" Background="{StaticResource BrushAccentBlue}" CornerRadius="8" Padding="8,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Rank, Converter={StaticResource RankToTextConverter}}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                                            </Border>

                                            <!-- Stats with Disclosure Check -->
                                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                                <TextBlock Text="⚔" FontSize="10" Foreground="{StaticResource BrushAccentRed}" HorizontalAlignment="Center"/>
                                                <TextBlock HorizontalAlignment="Center" FontWeight="Bold">
                                                    <TextBlock.Text>
                                                        <MultiBinding Converter="{StaticResource DisclosureConverter}">
                                                            <Binding Path="AbilityCombat"/>
                                                            <Binding Path="AbilityDisclosure.CombatDisclosed"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>

                                            <StackPanel Grid.Column="3" VerticalAlignment="Center">
                                                <TextBlock Text="🛡" FontSize="10" Foreground="{StaticResource BrushAccentBlue}" HorizontalAlignment="Center"/>
                                                <TextBlock HorizontalAlignment="Center" FontWeight="Bold">
                                                    <TextBlock.Text>
                                                        <MultiBinding Converter="{StaticResource DisclosureConverter}">
                                                            <Binding Path="AbilityLeadership"/>
                                                            <Binding Path="AbilityDisclosure.LeadershipDisclosed"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>

                                            <StackPanel Grid.Column="4" VerticalAlignment="Center">
                                                <TextBlock Text="📜" FontSize="10" Foreground="{StaticResource BrushAccentGreen}" HorizontalAlignment="Center"/>
                                                <TextBlock HorizontalAlignment="Center" FontWeight="Bold">
                                                    <TextBlock.Text>
                                                        <MultiBinding Converter="{StaticResource DisclosureConverter}">
                                                            <Binding Path="AbilityPolitics"/>
                                                            <Binding Path="AbilityDisclosure.PoliticsDisclosed"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>

                                            <StackPanel Grid.Column="5" VerticalAlignment="Center">
                                                <TextBlock Text="🎯" FontSize="10" Foreground="{StaticResource BrushBorder}" HorizontalAlignment="Center"/>
                                                <TextBlock HorizontalAlignment="Center" FontWeight="Bold">
                                                    <TextBlock.Text>
                                                        <MultiBinding Converter="{StaticResource DisclosureConverter}">
                                                            <Binding Path="AbilityIntrigue"/>
                                                            <Binding Path="AbilityDisclosure.IntrigueDisclosed"/>
                                                        </MultiBinding>
                                                    </TextBlock.Text>
                                                </TextBlock>
                                            </StackPanel>

                                            <!-- Appoint Button -->
                                            <Button Grid.Column="7" Content="任命" Command="{Binding DataContext.AppointCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                                    CommandParameter="{Binding Id}" Padding="15,5"
                                                    IsEnabled="{Binding IsAdvisor, Converter={StaticResource InverseBooleanConverter}}"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Right Sidebar -->
        <Border Grid.Column="1" Background="{StaticResource BrushBgPanel}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2,0,0,0" Padding="20">
            <ScrollViewer>
                <StackPanel>
                    <!-- Help Info -->
                    <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="15" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="■ 補佐官について" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushAccentRed}" Margin="0,0,0,12"/>
                            <TextBlock Text="補佐官は家臣であれば誰でも任命可能です。" TextWrapping="Wrap" Margin="0,0,0,10"/>
                            <TextBlock Text="効果:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <TextBlock Text="・政務・知略能力が補正される" Margin="10,0,0,5"/>
                            <TextBlock Text="・全能力が即座に開示" Margin="10,0,0,5"/>
                            <TextBlock Text="・忠誠 +5" Margin="10,0,0,0"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
