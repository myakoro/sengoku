<UserControl x:Class="SengokuSLG.Views.SquadDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Background="{StaticResource BrushBgBase}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,2" Padding="20,15">
            <Grid>
                <TextBlock FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" VerticalAlignment="Center">
                    <Run Text="🗡 "/>
                    <Run Text="{Binding SelectedSquad.Name}"/>
                    <Run Text=" - 小隊詳細"/>
                </TextBlock>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="編集" Style="{StaticResource ButtonPrimary}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20">
            <StackPanel>
                <!-- Squad Summary -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                    <Grid>
                        <TextBlock FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}">
                            <Run Text="小頭: "/>
                            <Run Text="{Binding SelectedSquad.CommanderName}"/>
                            <Run Text=" / 総兵力: " Foreground="{StaticResource BrushTextSecondary}"/>
                            <Run Text="{Binding SelectedSquad.CombatantCount, Mode=OneWay}" Foreground="{StaticResource BrushTextSecondary}"/>
                            <Run Text="人 (徒士" Foreground="{StaticResource BrushTextSecondary}"/>
                            <Run Text="{Binding SelectedSquad.AshigaruCount, Mode=OneWay}" Foreground="{StaticResource BrushTextSecondary}"/>
                            <Run Text=" + 馬上衆" Foreground="{StaticResource BrushTextSecondary}"/>
                            <Run Text="{Binding SelectedSquad.MountedCount, Mode=OneWay}" Foreground="{StaticResource BrushTextSecondary}"/>
                            <Run Text=") / 所属: " Foreground="{StaticResource BrushTextSecondary}"/>
                            <Run Text="不明" Foreground="{StaticResource BrushAccentBlue}" FontWeight="Bold"/>
                        </TextBlock>
                        <Border Background="#d4edda" CornerRadius="6" Padding="10,8" HorizontalAlignment="Right">
                            <TextBlock FontSize="18" FontWeight="Bold" Foreground="#155724">
                                <Run Text="有効戦闘力: "/>
                                <Run Text="{Binding SelectedSquad.BasePower, Mode=OneWay}"/>
                            </TextBlock>
                        </Border>
                    </Grid>
                </Border>

                <!-- Combat Power Breakdown -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="戦力内訳" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,5"/>
                        <TextBlock FontSize="12" Foreground="{StaticResource BrushTextSecondary}">
                            <Run Text="基礎戦力: "/>
                            <Run Text="{Binding SelectedSquad.BasePower, Mode=OneWay}"/>
                            <Run Text=" / 訓練度: "/>
                            <Run Text="{Binding SelectedSquad.Training, Mode=OneWay}"/>
                            <Run Text=" / 士気: "/>
                            <Run Text="{Binding SelectedSquad.Morale, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <!-- Unit Lists -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Infantry List -->
                    <Border Grid.Column="0" Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,10,0">
                        <StackPanel>
                            <TextBlock Text="⚔ 徒士" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            <DataGrid ItemsSource="{Binding SelectedSquad.AshigaruList, Mode=OneWay}" AutoGenerateColumns="False" 
                                      Background="White" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                                      RowBackground="White" AlternatingRowBackground="#f9f9f9"
                                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="名前" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="戦闘力" Binding="{Binding CombatPower}" Width="60"/>
                                    <DataGridTextColumn Header="出身" Binding="{Binding OriginVillageId}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>

                    <!-- Cavalry List -->
                    <Border Grid.Column="1" Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                        <StackPanel>
                            <TextBlock Text="🐴 馬上衆" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                            <DataGrid ItemsSource="{Binding SelectedSquad.MountedList, Mode=OneWay}" AutoGenerateColumns="False" 
                                      Background="White" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                                      RowBackground="White" AlternatingRowBackground="#f9f9f9"
                                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="名前" Binding="{Binding Name}" Width="*"/>
                                    <DataGridTextColumn Header="戦闘力" Binding="{Binding CombatPower}" Width="60"/>
                                    <DataGridTextColumn Header="出身" Binding="{Binding OriginVillageId}" Width="80"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Servants -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                    <StackPanel>
                        <TextBlock Text="👥 従僕" FontSize="16" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding SelectedSquad.ServantList, Mode=OneWay}" AutoGenerateColumns="False" 
                                  Background="White" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                                  RowBackground="White" AlternatingRowBackground="#f9f9f9"
                                  CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="名前" Binding="{Binding Name}" Width="150"/>
                                <DataGridTextColumn Header="出身" Binding="{Binding OriginVillageId}" Width="80"/>
                                <DataGridTextColumn Header="戦闘力" Binding="{Binding CombatPower}" Width="80"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
