<UserControl x:Class="SengokuSLG.Views.VillageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TabControl Background="Transparent" BorderThickness="0">
            <TabItem Header="村A">
                <Border Background="{StaticResource BrushPanelBg}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="20" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="■村A 詳細データ" Style="{StaticResource TextStyleSectionTitle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="人口:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Service.VillageA.Population}" VerticalAlignment="Center" FontWeight="Bold"/>

                            <TextBlock Grid.Row="1" Text="治安:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Service.VillageA.Security}" VerticalAlignment="Center" FontWeight="Bold"/>

                            <TextBlock Grid.Row="2" Text="収入:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Service.VillageA.Income, StringFormat='{}{0}貫'}" VerticalAlignment="Center" FontWeight="Bold"/>

                            <TextBlock Grid.Row="3" Text="開発度:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Service.VillageA.Development}" VerticalAlignment="Center" FontWeight="Bold"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </TabItem>
            <TabItem Header="村B">
                <Border Background="{StaticResource BrushPanelBg}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="8" Padding="20" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="■村B 詳細データ" Style="{StaticResource TextStyleSectionTitle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                                <RowDefinition Height="40"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="人口:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Service.VillageB.Population}" VerticalAlignment="Center" FontWeight="Bold"/>

                            <TextBlock Grid.Row="1" Text="治安:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Service.VillageB.Security}" VerticalAlignment="Center" FontWeight="Bold"/>

                            <TextBlock Grid.Row="2" Text="収入:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Service.VillageB.Income, StringFormat='{}{0}貫'}" VerticalAlignment="Center" FontWeight="Bold"/>

                            <TextBlock Grid.Row="3" Text="開発度:" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Service.VillageB.Development}" VerticalAlignment="Center" FontWeight="Bold"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </TabItem>
            </TabControl>
    </Grid>
</UserControl>
