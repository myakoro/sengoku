<UserControl x:Class="SengokuSLG.Views.BattleResultSummaryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1080">
    <Grid Background="{StaticResource BrushBgBase}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BrushBgHeader}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,2" Padding="20,15">
            <Grid>
                <TextBlock Text="🎉 戦闘結果" FontSize="24" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="戻る" Command="{Binding BackCommand}" Style="{StaticResource ButtonSecondary}" Width="100" Margin="0,0,10,0"/>
                    <Button Content="詳細" Style="{StaticResource ButtonPrimary}" Width="100"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="900">
                <!-- 勝敗表示 -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                    <Grid>
                        <Border HorizontalAlignment="Left">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#66BB6A" Offset="0"/>
                                    <GradientStop Color="#43A047" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border CornerRadius="6" Padding="15,30">
                                <TextBlock Text="🎉 大勝" FontSize="32" FontWeight="Bold" Foreground="White"/>
                            </Border>
                        </Border>
                        <TextBlock HorizontalAlignment="Right" VerticalAlignment="Center" FontSize="15">
                            <Run Text="自軍: " FontWeight="Bold"/>
                            <Run Text="300→270人(損耗30) / "/>
                            <Run Text="敵軍: " FontWeight="Bold"/>
                            <Run Text="300→200人(損耗100) / 補正×1.3"/>
                        </TextBlock>
                    </Grid>
                </Border>

                <!-- 損耗総計 -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="損耗総計" FontSize="14" FontWeight="Bold" Foreground="{StaticResource BrushTextMain}" Margin="0,0,0,10"/>
                        <Grid TextElement.FontSize="14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0">
                                <Run Text="自軍: " FontWeight="Bold"/>
                                <Run Text="死亡5 / 廃兵３ / 負傷18 / 逃散４ = 計30人"/>
                            </TextBlock>
                            <TextBlock Grid.Column="1" Foreground="#C62828">
                                <Run Text="敵軍: " FontWeight="Bold"/>
                                <Run Text="死亡25 / 廃兵15 / 負傷45 / 逃散15 = 計100人"/>
                            </TextBlock>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 戦功集計 -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15" Margin="0,0,0,12">
                    <Grid>
                        <TextBlock FontSize="14" VerticalAlignment="Center">
                            <Run Text="戦功集計: " FontWeight="Bold"/>
                            <Run Text="士気+450 / 崩壊+320 / 追撃+280 / 効率+200 = 1,250点 × 1.3（大勝）− 35（借兵ペナ）"/>
                        </TextBlock>
                        <Border Background="#d4edda" CornerRadius="6" Padding="10,25" HorizontalAlignment="Right">
                            <TextBlock Text="確定: 1,590点" FontSize="28" FontWeight="Bold" Foreground="#155724"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- アクションボタン -->
                <Border Background="{StaticResource BrushBgCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="2" CornerRadius="6" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="損耗詳細レポート" Style="{StaticResource ButtonSecondary}" Padding="15" FontSize="16"/>
                        <Button Grid.Column="2" Content="戦功詳細レポート" Style="{StaticResource ButtonSecondary}" Padding="15" FontSize="16"/>
                        <Button Grid.Column="4" Content="メインへ戻る" Style="{StaticResource ButtonPrimary}" Padding="15" FontSize="16"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
